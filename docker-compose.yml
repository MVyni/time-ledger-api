services:
  time-ledger:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=${POSTGRES_USER:-docker}
      - POSTGRESQL_PASSWORD=${POSTGRES_PASSWORD:-docker}
      - POSTGRESQL_DATABASE=${POSTGRES_DB:-timeledger}